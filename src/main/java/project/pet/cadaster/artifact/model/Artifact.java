package project.pet.cadaster.artifact.model;

import javax.persistence.Entity;
import javax.persistence.Table;

import project.pet.cadaster._common.model.DescribedEntity;

@Entity
@Table(name = "ARTIFACT")
public class Artifact extends DescribedEntity {
	
}
